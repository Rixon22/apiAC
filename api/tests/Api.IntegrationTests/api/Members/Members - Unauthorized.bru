meta {
  name: Members - Unauthorized
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/members
  body: none
  auth: inherit
}

tests {
    test("Should return unauthorized", function() {
      expect(res.getStatus()).to.equal(401);
    });
}

settings {
  encodeUrl: true
  timeout: 0
}
