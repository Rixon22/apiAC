meta {
  name: Login - Validate
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "arenita@test.com",
    "password": "password"
  }
}

tests {
  test("Should be able to login", function() {
    expect(res.getStatus()).to.equal(200);
  });
  const data = res.body;
  bru.setEnvVar("authToken", data.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
